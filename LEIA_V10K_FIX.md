# DeckDeckDeco v10k-fix

Mudancas importantes:

- `Smart Button` agora mostra so um seletor de perfil no painel pequeno do Ulanzi.
- O painel web completo continua abrindo pelo `Control Panel` no navegador.
- `Smart Button` usa a imagem/GIF do perfil e nao troca para READY/DONE/SAVED sozinho.
- Para criar ou editar perfis, use o painel web completo.
- Para usar no deck, arraste `Smart Button` e escolha o perfil A/B/C/etc no seletor pequeno.

Instalacao:

```powershell
npm run setup:windows
```
